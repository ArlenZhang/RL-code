    Q表存储数据很传统，但是对实际问题的复杂面前，Q表不能解决问题，产生 DQN-Deep Q Network.
                    Deep Neural Network + QLearning
方案一：
    将状态和动作作为神经网络的输入，经过神经网路后输出得到Q值，这样就没有必要用Q表存储Q值，而用一组神经网络
参数计算得到。
方案二：
    仅仅将状态作为输入经神经网络后输出所有的动作值并取最大值的动作（可以通过训练得到该的功能神经网络）。